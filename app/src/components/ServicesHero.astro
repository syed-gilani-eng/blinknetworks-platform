---
---

<section class="services-hero" aria-labelledby="services-hero-heading">
  <div class="services-hero__inner">
    <div class="services-hero__content">
      <p class="services-hero__eyebrow">OUR SERVICES</p>
      <h1 id="services-hero-heading" class="services-hero__heading">
        Technology that keeps your business moving
      </h1>
      <p class="services-hero__lead">
        Secure, reliable and well-managed technology&mdash;delivered by a
        Canadian-owned IT partner serving growing businesses across Toronto
        and the GTA.
      </p>
      <div class="services-hero__actions">
        <a class="services-hero__cta services-hero__cta--primary" href="/contact/">Book a Consultation</a>
        <a class="services-hero__cta services-hero__cta--secondary" href="#services-list">Explore Our Services</a>
      </div>
    </div>

    <!-- Approved production hero illustration; decorative, since the adjacent copy communicates the page meaning. -->
    <div class="services-hero__visual">
      <img
        class="services-hero__image"
        src="/images/services/services-hero.webp"
        width="1942"
        height="809"
        alt=""
        decoding="async"
        fetchpriority="high"
      />
    </div>
  </div>
</section>

<style>
  .services-hero {
    background-color: var(--color-blink-navy);
    color: var(--color-white);
    overflow: hidden;
  }

  .services-hero__inner {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding-mobile);
    padding-block: 40px;
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  @media (min-width: 640px) {
    .services-hero__inner {
      padding-inline: var(--container-padding-tablet);
      padding-block: 40px 48px;
    }
  }

  @media (min-width: 960px) {
    .services-hero__inner {
      padding-inline: var(--container-padding-desktop);
      padding-block: 56px;
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      align-items: center;
      gap: 40px;
    }
  }

  .services-hero__content {
    max-width: 46ch;
  }

  @media (min-width: 960px) {
    .services-hero__content {
      max-width: none;
    }
  }

  .services-hero__eyebrow {
    margin: 0 0 16px;
    color: var(--color-network-cyan);
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .services-hero__heading {
    margin: 0 0 20px;
    font-size: 2.25rem;
    line-height: 1.15;
    font-weight: 700;
  }

  @media (min-width: 960px) {
    .services-hero__heading {
      font-size: var(--font-size-h2);
    }
  }

  .services-hero__lead {
    margin: 0 0 32px;
    max-width: 48ch;
    color: var(--color-soft-azure);
    font-size: 1.0625rem;
    line-height: 1.6;
  }

  .services-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
  }

  .services-hero__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding-inline: 24px;
    border-radius: var(--radius-button);
    font-weight: 600;
    text-decoration: none;
    transition: background-color var(--duration-fast) ease,
      border-color var(--duration-fast) ease,
      color var(--duration-fast) ease;
  }

  /* Blink Blue does not reliably meet AA behind white text; Network Cyan + navy text does. */
  .services-hero__cta--primary {
    background-color: var(--color-network-cyan);
    color: var(--color-blink-navy);
    border: 1px solid var(--color-network-cyan);
  }

  .services-hero__cta--primary:hover {
    background-color: var(--color-soft-azure);
    border-color: var(--color-soft-azure);
  }

  .services-hero__cta--secondary {
    background-color: transparent;
    color: var(--color-white);
    border: 1px solid var(--color-white);
  }

  .services-hero__cta--secondary:hover {
    border-color: var(--color-network-cyan);
    color: var(--color-network-cyan);
  }

  /* Dark background: keep focus rings on the approved brighter accent rather than the default Blink Blue. */
  .services-hero :focus-visible {
    outline-color: var(--color-network-cyan);
  }

  /* Decorative only; visible at every breakpoint, sized so it never overpowers the heading or CTAs. */
  .services-hero__visual {
    width: 100%;
    max-width: 420px;
    margin-inline: auto;
  }

  .services-hero__image {
    display: block;
    width: 100%;
    height: auto;
    object-fit: contain;
  }

  @media (min-width: 960px) {
    .services-hero__visual {
      max-width: none;
      align-self: center;
    }

    .services-hero__image {
      max-width: 460px;
      margin-inline: auto;
    }
  }
</style>
